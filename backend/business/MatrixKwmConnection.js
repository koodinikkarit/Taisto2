var matrixsKwmConnections = { };

var nextId = 0;

export default class MatrixKwmConnection {
	static async gen(id) {

	}

	static async del(id) {

	}

	static async new({
		cpuPort, 
		value }) {
			
	}
}